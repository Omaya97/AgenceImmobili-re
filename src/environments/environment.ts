// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
};
export const firebaseConfig = {
  apiKey: "AIzaSyD5r42ga3765VFAqbVvvvLdaAgN0UmEHJs",
  authDomain: "gesannonces.firebaseapp.com",
  databaseURL: "https://gesannonces-default-rtdb.firebaseio.com",
  projectId: "gesannonces",
  storageBucket: "gesannonces.appspot.com",
  messagingSenderId: "914413630993",
  appId: "1:914413630993:web:06dc32cc750218f69ba3b0"
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
